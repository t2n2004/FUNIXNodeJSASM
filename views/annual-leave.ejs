<%- include('includes/head.ejs') %>
</head>

<body>
    <%- include('./includes/navigations.ejs') %>
    <h1>Xin chào <%= staff.name %></h1>

    <main>
        <article class="card product-item">
            <header class="card__header">
                <h1 class="product__title">Apply for Annual Leave</h1>
            </header>

        <div class="card__content">
            <p>Annual Leave: <%= staff.annualLeave %></p>
        </div>

        <div class="card__content">
            <p>Available Annual Leave: </p>
        </div>

        <form class="product-form" action="/staff/annual-leave" method="POST">
            <div class="form-control">
                <label for="startDate">Start Leave</label>
                <input type="datetime" name="startDate" id="startDate" value="<%= staff.startDate %>">
            </div>
            <div class="form-control">
                <label for="startDate">End Leave</label>
                <input type="datetime" name="startDate" id="startDate" value="<%= staff.startDate %>">
            </div>

            <input type="hidden" name="_csrf" value="<%= csrfToken %>">
            <button class="btn" type="submit">Apply</button>
        </form>

        <div>
            <% if (leaves.length == 0) { %>
                <p>Take no leave!</p>
            <%} else { %>
                <p>Total Leave: <%= total / 1000 / 3600 %> gio</p>
            <% } %>
        
            
        </div>

    </article>
       
    </main>
    

</body>

<%- include('includes/end.ejs') %>